# Copy to .env and fill in your watch's adb device id so install.sh
# doesn't have to guess when multiple devices/emulators are attached.
# Find it with `adb devices` while only your watch is connected.
ADB_DEVICE_ID=
